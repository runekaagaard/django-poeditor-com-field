============
Installation
============

At the command line::

    $ easy_install django-poeditor-com-field

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-poeditor-com-field
    $ pip install django-poeditor-com-field
